-- ============================================
-- 로컬 DB 스키마 내보내기 가이드
-- ============================================
-- 이 파일은 가이드입니다. 실제 내보내기는 아래 방법을 사용하세요.
-- ============================================

-- 방법 1: phpMyAdmin 사용
-- 1. http://localhost/phpmyadmin 접속
-- 2. 왼쪽에서 'mvno_db' 데이터베이스 선택
-- 3. 상단 "내보내기" 탭 클릭
-- 4. "빠른" 또는 "사용자 정의" 선택
-- 5. "구조만" 또는 "구조와 데이터" 선택
-- 6. "실행" 클릭하여 SQL 파일 다운로드

-- 방법 2: 명령줄 사용 (구조만)
-- mysqldump -u root -p --no-data mvno_db > local_schema_structure_only.sql

-- 방법 3: 명령줄 사용 (구조 + 데이터)
-- mysqldump -u root -p mvno_db > local_schema_with_data.sql

-- 방법 4: DBeaver 사용
-- 1. mvno_db 연결 선택
-- 2. 우클릭 → "도구" → "데이터베이스 내보내기"
-- 3. "구조만" 또는 "구조와 데이터" 선택
-- 4. SQL 파일로 저장
